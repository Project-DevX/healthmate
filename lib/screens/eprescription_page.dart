import 'package:flutter/material.dart';
class EPrescriptionPage extends StatelessWidget {
  const EPrescriptionPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('E-Prescription')),
      body: const Center(child: Text('E-Prescription Page (Placeholder)')),
    );
  }
} 
